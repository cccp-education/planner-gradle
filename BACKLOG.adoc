= Backlog — Manhattan (planner-gradle)
:description: Backlog de publication Maven Central — EPIC PUB + EPIC PLN-LIFECYCLE
:date: 2026-08-17
:toc: left
:sectnums:

_Source : configuration/EPIC_PUB.adoc (Session 096) + Session 074 (cadrage PLN-LIFECYCLE)_

'''
== 🟡 EPIC PLN-LIFECYCLE — Planner Agent Lifecycle-Aware (Session 076 — US-2 Wiring, 3/4 US)

_Source_ : Session 074. Split-brain #3 reporté S-061 :
`agent-contracts.AgentState` (8 phases sealed) + `ContextAssembler`/`PlanExecutor`
N0 (publié 0.0.3) sont des contrats orphan — zéro consommateur. Codebase gère
son lifecycle koog local (`KoogAugmentedContextGraph` 3 nodes + `AugmentedState`/
`PlanState` locaux). Planner est stateless. EPIC planner-only : exposer une API
lifecycle-aware additive (surcharge optionnelle `IntentionPlanner.plan(AgentState)`),
sans casser le contrat `PlannerIntegration` codebase.

_Dépendances_ : MEMPHIS (agent-contracts 0.0.3 déjà dans classpath planner,
zéro bump BOM). Codebase HORS SCOPE (borough DORMANT — EPIC futur migration
`AugmentedState`/`PlanState` → `AgentState`).

=== Audit — 6 constats

[cols="1,3,3", options="header"]
|===
| # | Constat | Preuve

| 1
| `AgentState` N0 est un contrat orphan
| `workspace-bom/agent-contracts/.../AgentState.kt` — 8 phases sealed (Initial/ContextReady/Classified/Planned/Executed/Evaluated/Finalized), publié 0.0.3. ZÉRO consommateur.

| 2
| `ContextAssembler`/`PlanExecutor` N0 sont des interfaces orphan
| `workspace-bom/agent-contracts/.../ContextAssembler.kt` — 2 interfaces, publié 0.0.3. ZÉRO implémentation.

| 3
| Codebase gère son propre lifecycle koog local
| `codebase-plugin/.../KoogAugmentedContextGraph.kt` — graphe koog 3 nodes + `AugmentedState` local. Ne consomme pas `AgentState` N0.

| 4
| `PlanState` est un état local codebase, pas N0
| `codebase-plugin/.../KoogPlan.kt:13` — `data class PlanState` redondant avec `AgentState.Planned` N0.

| 5
| Planner est stateless — aucun lifecycle
| `planner-plugin/.../DecomposeIntentionTask.kt:64` — reçoit intention + contexte, appelle `IntentionPlanner.plan()`, retourne `Plan`. Aucune phase, aucun état.

| 6
| Le split-brain est local-vs-N0, pas dumb-vs-smart
| `AgentState` N0 (8 phases + transitions + error + replanCount) strictement plus général que `PlanState`/`AugmentedState` locaux.
|===

=== Décisions architecturales (cadrage S-074)

. *Portée EPIC* : planner expose une API lifecycle-aware optionnelle.
  `IntentionPlanner.plan()` accepte un `AgentState.ContextReady` et retourne
  un `AgentState.Planned`. Backward compat : path actuel (4 String + Logger)
  préservé — l'API lifecycle-aware est additive, pas destructive.
. *Codebase HORS SCOPE initial* : borough DORMANT. La migration
  `AugmentedState`/`PlanState` → `AgentState` est un EPIC futur codebase.
. *Dépendance cross-borough BOM* : aucune nouvelle publication —
  `agent-contracts` 0.0.3 déjà dans le classpath de planner. Zéro bump BOM.
. *Adapter pattern* : `LifecycleAdapter` (object pur, domaine
  `planning.lifecycle`) mappe `PlanningContext` + 4 canaux →
  `AgentState.ContextReady` et `Plan` → `AgentState.Planned`.
. *Fallback sécurité* : si `AgentState` indisponible (exception) →
  `null` (path actuel préservé).
. *Non-régression critique* : `PlannerIntegration.kt` (codebase) appelle
  `IntentionPlanner.plan(4 String + Logger)`. Contrat d'appel inchangé.
. *Testabilité* : `AgentState` N0 est pure (sealed class immutable).
  `LifecycleAdapter` est pur. Tests unit + functional + Cucumber BDD.
. *Méthode* : baby-step DDD/TDD/BDD, 3 niveaux tests/US
  (unit → functional → cucumber), English only code/Gherkin.
. *Ordre* : US-1 (adapter, indépendant) → US-2 (wiring) → US-3 (Cucumber).

=== US découpage

[cols="1,3,1,1", options="header"]
|===
| US | Description | Pts | Statut

| PLN-LIFECYCLE-0
| *Cadrage EPIC* — audit planner + BOM + codebase, 6 constats, décisions, découpage US. Maj INDEX/BACKLOG/PROMPT_REPRISE/AGENTS racine. Création `CADRAGE_EPIC_PLN_LIFECYCLE.adoc` | 1
| ✅ TERMINÉ (S-074)

| PLN-LIFECYCLE-1
| *`LifecycleAdapter`* (domaine `planning.lifecycle`, nouveau) — object pur qui mappe `PlanningContext` + 4 canaux (`eagerContext`/`ragContext`/`graphifyContext`/`docsContext`) → `AgentState.ContextReady` (N0) et `Plan` → `AgentState.Planned` (N0). Fallback : si `AgentState` indisponible (exception) → `null` (path actuel préservé). ~8 unit | 2
| ✅ TERMINÉ (S-075)

| PLN-LIFECYCLE-2
| *Wiring surcharge optionnelle `IntentionPlanner.plan()`* — ajouter une surcharge `plan(state: AgentState.ContextReady, ...): AgentState.Planned` à `IntentionPlanner`. Backward compat : surcharge existante `plan(intention, context, specContents, 4 String, model, logger)` préservée. `DecomposeIntentionTask` peut consommer l'API lifecycle-aware si `-Pagent.lifecycle=true` (flag optionnel, défaut `false`). ~5 unit + 2 functional | 3
| ✅ TERMINÉ (S-076)

| PLN-LIFECYCLE-3
| *Cucumber BDD* — `planner_lifecycle.feature` : scénarios (intention → ContextReady → Planned via lifecycle adapter, fallback path actuel si `AgentState` indisponible, non-régression 4 String préservé, flag `-Pagent.lifecycle` optionnel). ~4 scénarios. Inspiré slider Cucumber pipeline + pattern `PlannerVerifySteps` shared state | 3
| 🟡 À FAIRE
|===

_Total_ : 4 US, ~9 pts. _Méthode_ : baby-step DDD/TDD/BDD, 3 niveaux
tests/US (unit → functional → cucumber), English only code/Gherkin.
_Dépendances_ : MEMPHIS (agent-contracts 0.0.3 déjà publié, zéro bump).
_Statut_ : 3/4 US TERMINÉ (S-074 cadrage + S-075 US-1 `LifecycleAdapter` ✅ + S-076 US-2 Wiring ✅). reste US-3 🟡 À FAIRE.

_Non-regression critique_ : `PlannerIntegration.kt` (codebase) appelle
`IntentionPlanner.plan(eagerContext, ragContext, graphifyContext, docsContext)`.
Le contrat d'appel (4 String + Logger → Plan) est préservé. L'API
lifecycle-aware est *additive* — une surcharge optionnelle, pas un
remplacement.

_Portée codebase (HORS SCOPE initial)_ : Codebase gère son propre lifecycle
koog (`KoogAugmentedContextGraph` + `AugmentedState` + `PlanState`). La
migration vers `AgentState` N0 est un EPIC futur codebase (borough Queens
DORMANT). Cet EPIC est *planner-only* — la portée planner est d'exposer
l'API lifecycle-aware pour que codebase puisse la consommer le jour venu.

'''
== EPIC PUB — Publication Maven Central

Plugin N2 de generation SPG/SPD via LLM (gpt-oss:120b-cloud). VAGUE 4 (plugins metier).

=== PUB-3.10 — planner-plugin
**Priorite** : P0
**Estimation** : 1 pt
**Dependances** : PUB-0 et PUB-1 complets (DNS TXT + GPG)
**Statut** : ✅ TERMINÉ (S-073)

. `cd foundry/public/planner-gradle && ./gradlew publish closeAndRelease`
. Plugin ID : `education.cccp.planner`
. Artefact : `education.cccp:planner-plugin`
. Version 0.0.2 publiée Maven Central (bundle `47064425-d42e-485c-b3f0-44245b623a97` AUTOMATIC)
. Verifier sur https://central.sonatype.com/artifact/education.cccp/planner-plugin

=== PUB-2.2 — Mise a jour commentaire relocation dans build.gradle.kts
**Priorite** : P1
**Estimation** : 1 pt
**Dependances** : Aucune
**Statut** : ✅ CADUQUE (S-073)

Le commentaire RELOCATION à mettre à jour a été supprimé lors de la migration
CNV-6 (session 056). La relocation est désormais gérée par la convention plugin
`education.ccp.build.publishing` via `publishingConventions { relocationGroupId = ... }`.
Rien à modifier.

'''
== ✅ EPIC LLM-ADAPTER-EXTRACT — Extraction LlmProviderChatModelAdapter vers N1 TERMINÉ (Session 073 — 6/6 US)

_Source_ : Session 072 découverte. `LlmProviderChatModelAdapter` (49 lignes)
était dupliqué à l'identique dans 3 boroughs N2 (slider, capsule, planner).
Extrait vers `codebase-plugin` (N1) package `codebase.koog.llm.adapter`.
`langchain4j` core promu `api` dans codebase pour visibilité transitive.
Bug préexistant `PlannerIntegration.kt` (build codebase cassé depuis S-072)
réparé.

_Dépendances_ : codebase-plugin 0.0.7 (publié Central bundle `e08acb47`).
Cross-borough 4 repos (Queens + MIAMI + Détroit + Manhattan).

[cols="1,3,1,1", options="header"]
|===
| US | Description | Pts | Statut

| LLM-ADAPTER-EXTRACT-0
| *Cadrage EPIC* — audit 3 copies, 6 constats, 10 décisions, découpage US. Maj INDEX/BACKLOG/PROMPT_REPRISE/AGENTS racine. Création `CADRAGE_EPIC_LLM_ADAPTER_EXTRACT.adoc` | 1
| ✅ TERMINÉ (S-073)

| LLM-ADAPTER-EXTRACT-1
| *Codebase N1* — Créer `codebase.koog.llm.adapter.LlmProviderChatModelAdapter` + 5 unit tests. `api(libs.langchain4j)` + `implementation` pgvector/minilm/ollama. Fix `PlannerIntegration.kt` (bug S-072). Bump 0.0.5→0.0.7. Publish Central bundle `e08acb47` | 3
| ✅ TERMINÉ (S-073)

| LLM-ADAPTER-EXTRACT-2
| *Slider consomme N1* — Supprimer copie locale + test. Import `codebase.koog.llm.adapter`. Aligner langchain4j 1.14.1→1.16.3. Bump codebase 0.0.7 | 2
| ✅ TERMINÉ (S-073)

| LLM-ADAPTER-EXTRACT-3
| *Capsule consomme N1* — Supprimer copie locale + test. Import aligné. Bump codebase 0.0.7 | 2
| ✅ TERMINÉ (S-073)

| LLM-ADAPTER-EXTRACT-4
| *Planner consomme N1* — Supprimer copie locale + test. Import aligné. Aligner langchain4j 1.16.3. Bump codebase 0.0.7. Wrapper autonome planner-plugin/ | 2
| ✅ TERMINÉ (S-073)

| LLM-ADAPTER-EXTRACT-5
| *Cucumber BDD planner* — `planner_adapter_extract.feature` 4 scénarios `@adapter-extract`. `PlannerAdapterExtractSteps` (steps préfixés "adapter extract" pattern S-088) | 3
| ✅ TERMINÉ (S-073)
|===

_Total_ : 6 US, ~13 pts. _Méthode_ : baby-step DDD/TDD/BDD, 3 niveaux
tests/US (unit → functional → cucumber), English only code/Gherkin.
_Statut_ : 6/6 US TERMINÉ (S-073). EPIC LLM-ADAPTER-EXTRACT CLOS.

'''
== ✅ EPIC PLN-LLM-HUB — Planner consomme codebase (N1) comme socle LLM unifié TERMINÉ (Session 072 — 4/4 US)

_Source_ : Session 072. Planner est le dernier borough N2 actif encore en
`OllamaBridge` standalone (singleton 24 lignes, instance unique
`localhost:11437`, zéro pool, zéro failover). slider (SLD-8 US-8.1/8.2)
et capsule (CAP-ARCH-1) ont déjà migré vers `codebase.koog.llm.service.LlmBuildService`
(Gradle BuildService DI, pool round-robin 11437-11465, quota failover,
cycling 2 modèles). Planner devient le 3e consommateur du hub N1 standardisé.

_Dépendances_ : codebase-plugin 0.0.5 (déjà publié Central, zéro bump BOM).

=== Articulation de la consommation

[cols="1,1,3,3", options="header"]
|===
| Niveau | Borough | Rôle | EPIC

| N0
| MEMPHIS
| Contrats (LlmInstancePool, RotationStrategy)
| MEM-2 llm-pool-contracts

| N1
| Queens (codebase)
| Hub LLM — LlmProviderResolver + OllamaPool + LlmBuildService
| SLD-8 US-8.1 (côté codebase)

| N2
| MIAMI (slider)
| 1er consommateur — AssistantManager + Adapter
| SLD-8 US-8.2

| N2
| Détroit (capsule)
| 2e consommateur — CapsuleLlmService + Adapter
| CAP-ARCH-1

| N2
| Manhattan (planner)
| 3e consommateur — PlanningLlmService + Adapter
| PLN-LLM-HUB (S-072)
|===

=== Audit — 6 constats

[cols="1,3,3", options="header"]
|===
| # | Constat | Preuve

| 1
| Planner a un bridge LLM standalone minimaliste
| `OllamaBridge.kt:1-24` — object singleton 24 lignes, `OllamaChatModel.builder()` direct, instance unique. Zéro pool, zéro failover.

| 2
| Port hardcoded 11437 (extrême plage autorisée)
| `OllamaBridge.kt:9`, `PlanningPlugin.kt:11`, `IntentionPlanner.kt:24,42,59`. Si 11437 tombe, build échoue.

| 3
| Modèle hardcoded `gemma4:31b-cloud` (un seul)
| `OllamaBridge.kt:10`, 4 sites. codebase cycle 2 modèles. Planner ne bénéficie pas du cycling.

| 4
| 3 surcharges `IntentionPlanner.plan()` construisent un ChatModel interne
| `IntentionPlanner.kt:26,44,62`. ChatModel non injectable — testabilité requiert mock HTTP.

| 5
| `DecomposeIntentionTask` passe 2 propriétés string (model/baseUrl)
| `DecomposeIntentionTask.kt:32-35`. Pas de `@ServiceReference`. Pattern pré-SLD-8.

| 6
| slider + capsule ont déjà migré, planner est le dernier holdout
| `AssistantManager.kt:75-113` + `CapsuleLlmService.kt:63-97` — pattern identique. Planner seul N2 actif en OllamaBridge standalone.
|===

=== Décisions architecturales (cadrage S-072)

. *Portée EPIC* : remplacer `OllamaBridge` par consommation
  `codebase.koog.llm.service.LlmBuildService`. Pool round-robin 11437-11465
  + quota failover + cycling 2 modèles. Zéro bump BOM.
. *Pattern consommateur* : mirror strict `CapsuleLlmService.kt` (113 lignes,
  le plus propre, sans legacy factories). Nouveau `PlanningLlmService`
  (domaine `planning.llm`) + `LlmProviderChatModelAdapter` (copy 49 lignes).
. *Backward compat mock-LLM* : `-Pollama.baseUrl` → `OllamaChatModel` mock
  (pattern slider/capsule). Production → `LlmBuildService` pool.
. *Injection ChatModel* : `IntentionPlanner.plan()` accepte `ChatModel`
  param au lieu de construire interne. 3 surcharges → 2 (avec/sans multi-channel).
. *Suppression `OllamaBridge.kt`* : 24 lignes + 2 tests null-check supprimés,
  remplacés par `PlanningLlmServiceTest` (10 tests supérieurs).
. *Suppression propriétés `ollamaModel`/`ollamaBaseUrl`* : remplacées par
  `@ServiceReference LlmBuildService` + `aiProvider` (`-Pai.provider`, défaut `"ollama"`).
. *Dépendance Gradle* : `implementation(libs.codebase.plugin)` ajouté.
  codebase-plugin 0.0.5 déjà Central. Pas de bump BOM racine.
. *Non-régression critique* : `PlannerIntegration.kt` (codebase) appelle
  `IntentionPlanner.plan(4 String + Logger)`. Contrat préservé.
. *Méthode* : baby-step DDD/TDD/BDD, 3 niveaux tests/US
  (unit → functional → cucumber), English only code/Gherkin.
. *Ordre* : US-1 (indépendant) → US-2 (wiring) → US-3 (Cucumber).

=== US découpage

[cols="1,3,1,1", options="header"]
|===
| US | Description | Pts | Statut

| PLN-LLM-HUB-0
| *Cadrage EPIC* — audit planner + codebase + slider/capsule, 6 constats, décisions, découpage US. Maj INDEX/BACKLOG/PROMPT_REPRISE/AGENTS racine. Création `CADRAGE_EPIC_PLN_LLM_HUB.adoc` | 1
| ✅ TERMINÉ (S-072)

| PLN-LLM-HUB-1
| *`PlanningLlmService` + `LlmProviderChatModelAdapter`* (domaine `planning.llm`, nouveau) — object pur mirror `CapsuleLlmService` : `registerLlmBuildService()` + `resolveModel(provider, serviceProvider)` + `aiProvider` extension + mock-LLM fallback `-Pollama.baseUrl`. Adapter copy slider/capsule (49 lignes). Dépendance `codebase-plugin` 0.0.5 ajoutée `build.gradle.kts`. ~10 unit | 3
| ✅ TERMINÉ (S-072)

| PLN-LLM-HUB-2
| *Wiring `DecomposeIntentionTask` + suppression `OllamaBridge`* — `IntentionPlanner.plan()` accepte `ChatModel` param (3 surcharges → 2). `DecomposeIntentionTask` remplace `ollamaModel`/`ollamaBaseUrl` par `@ServiceReference LlmBuildService` + `PlanningLlmService.resolveModel()`. `PlanningPlugin` enregistre le service. `OllamaBridge.kt` supprimé (24 lignes). `OllamaBridgeTest` supprimé, remplacé par `PlanningLlmServiceTest`. `PlannerExtension` : `ollamaModel`/`ollamaBaseUrl` → `aiProvider`. ~8 unit + 2 functional | 3
| ✅ TERMINÉ (S-072)

| PLN-LLM-HUB-3
| *Cucumber BDD* — `planner_llm_hub.feature` : scénarios (planner consomme LlmBuildService pool, mock-LLM fallback via -Pollama.baseUrl, ai.provider property ollama default, backward compat IntentionPlanner.plan contract preserved). ~4 scénarios. Inspiré slider `capsule_ai.feature` + pattern `PlannerVerifySteps` shared state | 3
| ✅ TERMINÉ (S-072)
|===

_Total_ : 4 US, ~10 pts. _Méthode_ : baby-step DDD/TDD/BDD, 3 niveaux
tests/US (unit → functional → cucumber), English only code/Gherkin.
_Dépendances_ : codebase-plugin 0.0.5 (déjà publié Central, zéro bump BOM).
_Statut_ : 4/4 US TERMINÉ (S-072 — cadrage + US-1 PlanningLlmService + US-2 wiring+suppression OllamaBridge + US-3 Cucumber BDD). EPIC PLN-LLM-HUB CLOS.

_Non-regression critique_ : `PlannerIntegration.kt` (codebase) appelle
`IntentionPlanner.plan(eagerContext, ragContext, graphifyContext, docsContext)`.
Le contrat d'appel (4 String + Logger → Plan) est préservé. Seul le
résolveur ChatModel change (pool au lieu de singleton).

'''
== ✅ EPIC PLN-BUDGET — Relevance Budget Dynamique TERMINÉ (Session 071 — US-3 Cucumber BDD)

_Source_ : Session 068. Split-brain #4 reporté S-061 :
`agent-contracts.RelevanceBudget` (N0, 0.0.3 déjà publié) fait un budget
pondéré par similarité (cosine/naive + softmax) que planner ignore.
`codebase-contracts.ChannelBudget` (fixe 40/30/20/10) est utilisé par planner
et codebase pour tronquer post-collecte. L'audit révèle que `RelevanceBudget`
est un contrat N0 orphan — le budget intelligent est disponible mais dormant.

_Dépendances_ : MEMPHIS (agent-contracts 0.0.3 déjà dans classpath planner,
zéro bump BOM). Codebase HORS SCOPE (borough DORMANT — EPIC planner-only).

=== Audit — 5 constats

[cols="1,3,3", options="header"]
|===
| # | Constat | Preuve

| 1
| `RelevanceBudget` est un contrat N0 orphan
| `workspace-bom/agent-contracts/.../RelevanceBudget.kt` — publié 0.0.3 (bundle `cd4d2d44`), 4 scénarios Cucumber `ac_4_relevance_budget.feature`, mais ZÉRO consommateur. Budget dynamique cosinus+softmax disponible mais dormant.

| 2
| `ChannelBudget` tronque avec proportions fixes
| `workspace-bom/codebase-contracts/.../ContextChannel.kt:117` — data class 5 fractions (40/30/20/10/0), `applyBudget(channels)` tronque post-collecte. Ne dépend pas de l'intention.

| 3
| Planner hardcode le budget fixe
| `planner-plugin/.../DecomposeIntentionTask.kt:104-121` — construit `CompositeContextConfig` (40/30/20/10 hardcodés), `ChannelBudget.fromConfig(config)`, `budget.applyBudget(channels)`. Un intention "planifier formation AFNOR" alloue 3200 tokens EAGER quoi que l'intention soit.

| 4
| Codebase fait pareil
| `codebase-plugin/.../CompositeContextBuilder.kt:32` + `MultiChannelContextGraph.kt:25` — `truncateTokens(eagerContent, config.eagerLazyTokens)` + `ChannelBudget()` defaults. Même budget fixe indépendant de l'intention.

| 5
| Le split-brain est fixe-vs-dynamique, pas dumb-vs-smart
| `RelevanceBudget.compute(intention, channels, totalTokens)` fait les deux (pondérer + tronquer) en un passage. `ChannelBudget` ne sait que tronquer. Les deux sont des budgets token — `RelevanceBudget` est strictement plus général.
|===

=== Décisions architecturales (cadrage S-068)

. *Portée EPIC* : faire consommer `RelevanceBudget` (agent-contracts 0.0.3
  déjà publié) par planner en remplaçant le budget fixe `ChannelBudget`
  par un budget dynamique dans `DecomposeIntentionTask`. Le budget dynamique
  prend l'intention + les contenus canaux et tronque selon la similarité.
. *Backward compat* : `ChannelBudget` reste pour les consommateurs sans
  pondération (tests, autres boroughs). `RelevanceBudget` est un upgrade
  optionnel — si l'intention est vide ou les embeddings indisponibles,
  fallback sur le contenu original (comportement actuel préservé).
. *Dépendance cross-borough BOM* : aucune nouvelle publication —
  `agent-contracts` 0.0.3 est déjà dans le classpath de planner
  (`build.gradle.kts:29`). Zéro bump BOM. `RelevanceBudget` est déjà
  disponible.
. *Dépendance cross-borough codebase* : HORS SCOPE. `CompositeContextBuilder`
  et `MultiChannelContextGraph` bénéficieront de `RelevanceBudget` dans un
  EPIC futur codebase. Borough Queens DORMANT.
. *Adapter pattern* : `RelevanceBudget.compute` prend `Map<String, String>`
  (nom canal → contenu) + `intention: String`. Planner a 4 canaux nommés
  (`eagerContext`, `ragContext`, `graphifyContext`, `docsContext`). Nouveau
  `BudgetAdapter` (object pur, domaine `planning.budget`) qui mappe
  4 canaux planner → Map → `compute` → `ChannelScore` → 4 String tronquées.
. *Fallback sécurité* : si `RelevanceBudget.compute` retourne vide ou lance,
  `BudgetAdapter` retourne les contenus originaux non tronqués (backward
  compat — le LLM reçoit le même contexte qu'aujourd'hui).
. *Non-régression critique* : `PlannerIntegration.kt` (codebase) appelle
  `IntentionPlanner.plan(eagerContext, ragContext, ...)`. Le contrat d'appel
  est inchangé — planner reçoit toujours 4 String. Seul le contenu tronqué
  change (dynamique au lieu de fixe).
. *Testabilité* : `RelevanceBudget.compute` est pure (aucun effet de bord).
  `BudgetAdapter` est pur aussi. Tests unit + functional + Cucumber BDD.
. *Méthode* : baby-step DDD/TDD/BDD, 3 niveaux tests/US
  (unit → functional → cucumber), English only code/Gherkin.
. *Ordre* : US-1 (adapter, indépendant) → US-2 (wiring) → US-3 (Cucumber).

=== US découpage

[cols="1,3,1,1", options="header"]
|===
| US | Description | Pts | Statut

| PLN-BUDGET-0
| *Cadrage EPIC* — audit planner + BOM + codebase, 5 constats, décisions, découpage US. Maj INDEX/BACKLOG/PROMPT_REPRISE/AGENTS racine | 1
| ✅ TERMINÉ (S-068)

| PLN-BUDGET-1
| *`BudgetAdapter`* (domaine `planning.budget`, nouveau) — object pur qui mappe 4 canaux planner (`eagerContext`/`ragContext`/`graphifyContext`/`docsContext`) → `Map<String, String>` (noms EAGER/RAG/GRAPHIFY/DOCS) → `RelevanceBudget.compute(intention, channels, totalTokens=8000)` → `List<ChannelScore>` → 4 String tronquées. Fallback : si compute retourne vide ou lance → contenus originaux non tronqués. ~8 unit | 2
| ✅ TERMINÉ (S-069)

| PLN-BUDGET-2
| *Wiring `DecomposeIntentionTask`* — remplacer `ChannelBudget.fromConfig(config)` + `budget.applyBudget(channels)` (lignes 104-121) par `BudgetAdapter.applyRelevanceBudget(intention, eagerCtx, ragCtx, graphifyCtx, docsCtx)`. Backward compat : si `RelevanceBudget` indisponible (exception) → fallback sur `ChannelBudget` fixe (path actuel). ~5 unit + 2 functional | 2
| ✅ TERMINÉ (S-070)

| PLN-BUDGET-3
| *Cucumber BDD* — `planner_budget.feature` : scénarios (budget dynamique alloue plus de tokens au canal pertinent, fallback fixe si similarity indisponible, non-régression 4 canaux préservés, total tokens respecté). ~4 scénarios. Inspiré slider Cucumber pipeline | 3
| ✅ TERMINÉ (S-071)
|===

_Total_ : 4 US, ~8 pts. _Méthode_ : baby-step DDD/TDD/BDD, 3 niveaux
tests/US (unit → functional → cucumber), English only code/Gherkin.
_Dépendances_ : MEMPHIS (agent-contracts 0.0.3 déjà publié, zéro bump).
_Statut_ : 4/4 US TERMINÉ (S-068 cadrage + S-069 US-1 `BudgetAdapter` ✅ + S-070 US-2 Wiring ✅ + S-071 US-3 Cucumber BDD ✅). EPIC PLN-BUDGET CLOS.

_Non-regression critique_ : `PlannerIntegration.kt` (codebase) appelle
`IntentionPlanner.plan(eagerContext, ragContext, graphifyContext, docsContext)`.
Le contrat d'appel est inchangé — planner reçoit toujours 4 String. Seul le
contenu tronqué change (dynamique au lieu de fixe). Si `RelevanceBudget.compute`
échoue ou retourne vide, fallback sur le contenu original non tronqué.

'''
== ✅ EPIC PLN-VERIFY — Planner Vibecoding Verification Metadata TERMINÉ (S-067)

_Source_ : Session 063 (cadrage). Split-brain #5 reporté en S-061 :
`codebase.koog.planning.VibecodingStep` a `expectedOutput`/`maxRetries`/
`verifyHook` que `contracts.agent.GradleTask` (N0) n'a pas. Le bridge
codebase `VibecodingGraph.extractCurrentStep` hardcode
`expectedOutput = "BUILD SUCCESSFUL"` — le StepVerifier ne peut pas
exploiter la vraie expectation de chaque tâche que planner connaît.

_Dépendances_ : MEMPHIS (agent-contracts 0.0.3), codebase (bridge 2
sites `VibecodingGraph.kt:713/741`).

=== Audit — 4 constats

[cols="1,3,3", options="header"]
|===
| # | Constat | Preuve

| 1
| `VibecodingStep` a 3 champs de vérification
| `codebase-plugin/.../VibecodingPlan.kt:10` — `expectedOutput: String`, `maxRetries: Int = 3`, `verifyHook: String? = null`. Consommé par `StepVerifier.verifyAndAdapt` (retry/give-up + prompt replan).

| 2
| `GradleTask` N0 n'a aucun champ de vérification
| `workspace-bom/agent-contracts/.../AgentState.kt:126` — 5 champs (description/gradleTask/toolType/target/project), 3 invariants. Pas d'expectedOutput/maxRetries/verifyHook.

| 3
| Bridge codebase hardcode les métadonnées manquantes
| `codebase-plugin/.../VibecodingGraph.kt:713` (extractCurrentStep) + `:741` (rollback) — `expectedOutput = "BUILD SUCCESSFUL"` constant, ignorant la nature de la tâche. Drift : `generateSPG` produit `"SPG generated"` mais vérifié contre `"BUILD SUCCESSFUL"`.

| 4
| Le prompt LLM ne demande pas les métadonnées
| `planner-plugin/.../IntentionPlanner.kt:135` — schéma JSON de sortie sans `expectedOutput`/`maxRetries`/`verifyHook`. Le LLM n'a aucun moyen de les fournir.
|===

=== Décisions architecturales (cadrage S-063)

. *Portée EPIC* : étendre `contracts.agent.GradleTask` (N0) avec 3 champs
  de vérification optionnels (defaults backward compat) + étendre le
  prompt LLM + étendre le bridge codebase. Pas de nouveau type — on
  enrichit le N0 existant (pattern PLN-CONTRACTS US-1).
. *Backward compat critique* : defaults préservent le comportement
  actuel — `expectedOutput = "BUILD SUCCESSFUL"`, `maxRetries = 3`,
  `verifyHook = null`. Avec ces defaults, le bridge codebase produit le
  même `VibecodingStep` qu'aujourd'hui → zéro régression.
. *Dépendance cross-borough BOM* : `agent-contracts` 0.0.2 → 0.0.3 (bump
  + 3 champs + invariants). Publication MEMPHIS.
. *Dépendance cross-borough codebase* : `VibecodingGraph.extractCurrentStep`
  + rollback (2 sites) remplacent le hardcodé par la vraie valeur du
  plan. Si planner ne fournit pas → default identique au comportement
  actuel.
. *Invariants N0* : `expectedOutput` non-blank, `maxRetries` 1..10
  (anti-abus LLM halluciné), `verifyHook` nullable.
. *Non-régression critique* : `PlannerIntegration.kt` (codebase) pont
  `Plan → PlanState → VibecodingStep` doit produire identique si LLM
  omet les métadonnées.
. *Méthode* : baby-step DDD/TDD/BDD, 3 niveaux tests/US
  (unit → functional → cucumber), English only code/Gherkin.
. *Ordre* : US-1 (BOM, indépendant) → US-2 (planner) → US-3 (codebase) →
  US-4 (Cucumber).

=== US découpage

[cols="1,3,1,1", options="header"]
|===
| US | Description | Pts | Statut

| PLN-VERIFY-0
| *Cadrage EPIC* — audit planner + codebase + BOM, 4 constats, décisions, découpage US. Maj INDEX/BACKLOG/PROMPT_REPRISE/AGENTS racine | 1
| ✅ TERMINÉ (S-063)

| PLN-VERIFY-1
| *BOM cross-borough* — Étendre `contracts.agent.GradleTask` avec `expectedOutput: String = "BUILD SUCCESSFUL"` + `maxRetries: Int = 3` + `verifyHook: String? = null`. Invariants `init` (expectedOutput non-blank, maxRetries 1..10). Bump `agent-contracts` 0.0.2 → 0.0.3. Publication Maven Central (MEMPHIS). Backward compatible. ~6 unit | 2
| ✅ TERMINÉ (S-064)

| PLN-VERIFY-2
| *Planner consomme N0 étendu* — Étendre `LLMTask` (expectedOutput/maxRetries/verifyHook nullables) + `LLMTask.toTask()` mapping avec defaults. `IntentionPlanner.buildPromptInternal` schéma JSON + règles prompt (demander expectedOutput, maxRetries optionnel, verifyHook rare). `StdoutFormatter` affiche expectedOutput. ~10 unit | 3
| ✅ TERMINÉ (S-065)

| PLN-VERIFY-3
| *codebase cross-borough* — `VibecodingGraph.extractCurrentStep` + rollback (2 sites) remplacent `"BUILD SUCCESSFUL"` hardcodé par `task.expectedOutput`/`task.maxRetries`/`task.verifyHook`. Non-régression : codebase koog tests + PlannerIntegration. ~3 unit | 1
| ✅ TERMINÉ (S-066)

| PLN-VERIFY-4
| *Cucumber BDD* — `planner_verify.feature` : scénarios (plan avec expectedOutput custom, maxRetries custom, verifyHook présent, defaults préservés si LLM omet, end-to-end planner → extractCurrentStep → StepVerifier). ~5 scénarios. Inspiré slider Cucumber pipeline | 3
| ✅ TERMINÉ (S-067)
|===

_Total_ : 5 US, ~10 pts. _Méthode_ : baby-step DDD/TDD/BDD, 3 niveaux
tests/US (unit → functional → cucumber), English only code/Gherkin.
_Dépendances_ : MEMPHIS (agent-contracts 0.0.3), codebase (bridge 2
sites). _Statut_ : 5/5 US TERMINÉ (S-063 cadrage + S-064 US-1 BOM 0.0.3 + S-065 US-2 planner + S-066 US-3 codebase bridge + S-067 US-4 Cucumber BDD). EPIC PLN-VERIFY CLOS.

_Non-regression critique_ : `PlannerIntegration.kt` (codebase) appelle
`IntentionPlanner.plan()` puis construit `PlanState`. Le pont
`Plan → VibecodingStep` via `extractCurrentStep` doit produire un
`VibecodingStep` identique à aujourd'hui si le LLM ne fournit pas les
métadonnées (defaults backward compat : `"BUILD SUCCESSFUL"`, `3`, `null`).

'''
== EPIC PUB-5 — Verification

=== PUB-5.1 — Verifier Maven Central

| Artefact | URL |
|---|---|
| planner-plugin | https://central.sonatype.com/artifact/education.cccp/planner-plugin |

'''
== EPIC PLN-VIBE — Planner Vibecoding-Aware

_Source_ : Session 057. Planner est le cerveau du vibecoding mais ne planifie
que des taches Gradle. `ToolRegistry` de codebase sait deja executer 7 outils
(read/write/edit_file, list_directory, exec_shell, exec_gradle, list_tasks).
Planner doit planifier multi-outil (edit/shell/gradle) et piloter les createurs
de contenu (slider/bakery/capsule/plantuml) via le hub codebase.

_Dependances_ : codebase (ToolRegistry catalogue), workspace-bom (AgentState/GradleTask).

[cols="1,3,1,1", options="header"]
|===
| US | Description | Pts | Statut
| PLN-VIBE-0 | Cadrage EPIC — vision, inventaire gap, decoupage US | 1 | ✅ TERMINÉ (S-057)
| PLN-VIBE-1 | `TaskType` enum (GRADLE, EDIT_FILE, EXEC_SHELL) + etendre `Task` (toolType, target). Invariants `init`. ~8 unit | 2 | ✅ TERMINÉ (S-058)
| PLN-VIBE-2 | `LLMTask` etendu (toolType, target) + `LLMResponse.toPlan()` mis a jour. ~6 unit | 2 | ✅ TERMINÉ (S-058)
| PLN-VIBE-3 | Prompt `IntentionPlanner` enrichi : catalogue outils + cross-borough. ~5 unit | 2 | ✅ TERMINÉ (S-058)
| PLN-VIBE-4 | `StdoutFormatter` etendu — affiche `toolType`. `DecomposeIntentionTask` passe nouveau modele. ~4 unit | 1 | ✅ TERMINÉ (S-058)
| PLN-VIBE-5 | Cucumber BDD — `planner_vibecoding.feature` : scenarios multi-outil. ~4 scenarios | 3 | ✅ TERMINÉ (S-059)
|===

_Total_ : 6 US, ~12 pts. _Methode_ : baby-step TDD/BDD, English only.
_Non-regression_ : `toolType` defaut `GRADLE` + `target` defaut `""` preserve
`PlannerIntegration.kt` (codebase).

== ✅ EPIC PLN-VIBE — Planner Vibecoding-Aware TERMINÉ (S-059)

EPIC clos 6/6 US (S-057 cadrage + S-058 US-1/2/3/4 + S-059 US-5 Cucumber BDD).
Planner est désormais vibecoding-aware : multi-outil (GRADLE/EDIT_FILE/
EXEC_SHELL), prompt catalogue 6 outils + cross-borough, non-régression
PlannerIntegration codebase préservée (default GRADLE).

'''
== ✅ EPIC PLN-CONTRACTS — Planner aligné sur les contrats N0 (BOM + codebase) TERMINÉ (S-061 + US-4 stricte S-062)

_Source_ : Session 061 — audit planner (11 fichiers, 624 lignes) +
dépendances cross-borough BOM (workspace-bom) + codebase (PlannerIntegration).
PLN-VIBE a rendu planner vibecoding-aware mais en ré-déclarant
localement des types et catalogues déjà fournis par les contrats N0.
Cet EPIC élimine le split-brain en faisant consommer à planner les
contrats N0 existants et en étendant le BOM là où il est trop étroit.
Session 062 — US-4 stricte : fusion `codebase.koog.planning.Plan` →
`contracts.agent.Plan` (le duplicat N1 est supprimé, codebase consomme
le N0 directement, bridge `toContractPlan()` supprimé car identité).

=== Audit — 5 points de split-brain

[cols="1,3,3", options="header"]
|===
| # | Split-brain | Constat

| 1
| Plan/Epic/UserStory/Task
| planner re-déclare 4 types déjà dans `agent-contracts` N0 (`contracts.agent.Epic/UserStory/GradleTask`). `GradleTask` N0 manque `toolType`/`target` (ajoutés localement par PLN-VIBE). Le bridge `planning.Plan.toContractPlan()` côté codebase DROPPAIT l'info vibecoding (ne mappe que `description`+`gradleTask`).

| 2
| Tool catalogue
| planner hardcode 6 outils en string dans `IntentionPlanner.buildPromptInternal`. `vibecoding-contracts.ToolRegistry` N0 expose 7 outils avec descriptions (`read_file`, `write_file`, `edit_file`, `list_directory`, `exit`, `exec_shell`, `exec_gradle`). Drift : planner dit 6, registry a 7.

| 3
| Agent lifecycle
| `IntentionPlanner` est stateless (plan → throw/return). `agent-contracts.AgentState` (8 phases sealed : BUILD_CONTEXT→CLASSIFY→PLAN→EXECUTE→EVALUATE→REPLAN→FINALIZE→GIVE_UP) + `ContextAssembler`/`PlanExecutor` N0 existent mais planner les ignore. codebase `KoogAugmentedContextGraph` appelle planner puis alimente `PlanState` à la main.

| 4
| Budget contexte
| planner utilise `codebase-contracts.ChannelBudget` (40/30/20/10 fixe). `agent-contracts.RelevanceBudget` N0 fait budget pondéré par similarité (cosine ou naive) — plus smart. planner utilise le budget dumb.

| 5
| VibecodingStep
| codebase `VibecodingPlan.VibecodingStep` a `expectedOutput`/`maxRetries`/`verifyHook` que planner `Task` n'a pas. planner planifie des tâches sans métadonnées de vérification que l'exécuteur codebase attend.
|===

=== Décisions architecturales (cadrage S-061)

. *Portée EPIC* : traiter les split-brain #1, #2, #4 (les plus sûrs et
  haute valeur). #3 (lifecycle) et #5 (VibecodingStep) reportés à un
  EPIC futur — ils changent le contrat d'exécution, pas juste les types.
. *Dépendance cross-borough BOM* : pour éliminer #1 totalement, étendre
  `contracts.agent.GradleTask` avec `toolType` + `target` (defaults
  `GRADLE`/`""`) + ajouter `TaskType` enum dans `agent-contracts`.
  Bump `agent-contracts` 0.0.1 → 0.0.2 + publication Maven Central
  (MEMPHIS). Backward compatible (default args Kotlin data class).
. *Dépendance cross-borough codebase* : une fois planner émettant des
  types N0 directement, le bridge `toContractPlan()` dans
  `codebase.rag.PlannerIntegration` devient identité → suppression.
. *Non-régression critique* : `PlannerIntegration.kt` (codebase) appelle
  `IntentionPlanner.plan()` puis `planningPlan.toContractPlan()`. Tant
  que planner expose un `Plan` convertible en `contracts.agent.Plan`,
  le contrat est préservé. L'US-2 garde un mapper `toContractPlan()` si
  US-1 non publiée.
. *Méthode* : baby-step DDD/TDD/BDD, 3 niveaux tests/US
  (unit → functional → cucumber), English only code/Gherkin.
. *Ordre* : US-3 (indépendant, single-borough) → US-1 (BOM cross-borough)
  → US-2 (planner consomme N0) → US-4 (codebase bridge) → US-5 (cucumber).

=== US découpage

[cols="1,3,1,1", options="header"]
|===
| US | Description | Pts | Statut

| PLN-CONTRACTS-0
| *Cadrage EPIC* — audit planner + BOM + codebase, 5 split-brain, décisions, découpage US. Maj INDEX/BACKLOG/PROMPT_REPRISE/AGENTS racine | 1 | ✅ TERMINÉ (S-061)

| PLN-CONTRACTS-1
| *BOM cross-borough* — Étendre `contracts.agent.GradleTask` avec `toolType: TaskType` + `target: String` (defaults `GRADLE`/`""`). Ajouter `enum class TaskType { GRADLE, EDIT_FILE, EXEC_SHELL }` dans `agent-contracts`. Bump 0.0.1 → 0.0.2. Publication Maven Central (MEMPHIS). Backward compatible. Tests invariants portés depuis planner `Task.init`. ~6 unit | 2 | ✅ TERMINÉ (S-061)

| PLN-CONTRACTS-2
| *Planner consomme N0* — Supprimer `Plan.kt` local (`Plan`/`Epic`/`UserStory`/`Task`/`TaskType`). Consommer `contracts.agent.Plan` (à créer si manquant) / `Epic`/`UserStory`/`GradleTask`/`TaskType` depuis `agent-contracts` 0.0.2. Update `LLMResponse.toPlan()`. Port invariants `Task.init` vers `GradleTask`. Si US-1 non publiée : garder mapper `toContractPlan()` local. ~8 unit | 3 | ✅ TERMINÉ (S-061)

| PLN-CONTRACTS-3
| *Tool catalogue from registry* — Remplacer le string hardcodé 6 outils dans `buildPromptInternal` par un adapter lisant `vibecoding-contracts.ToolRegistry` (`toolNames()` + `descriptions`). Nouveau `ToolCatalog` object. `IntentionPlanner.buildPrompt` consomme le catalogue. Dépend seulement de `vibecoding-contracts` (déjà publié). ~5 unit | 2 | ✅ TERMINÉ (S-061)

| PLN-CONTRACTS-4
| *codebase cross-borough* — Supprimer le bridge `planning.Plan.toContractPlan()` dans `codebase.rag.PlannerIntegration` (devenu identité après US-2). Non-régression : codebase koog tests + PlannerIntegration. ~3 unit | 1 | ✅ TERMINÉ (S-061 prudent + S-062 stricte fusion)

| PLN-CONTRACTS-5
| *Cucumber BDD* — `planner_contracts.feature` : scenarios (plan output N0-typed, tool catalogue from registry 7 outils, multi-tool tasks preserved end-to-end sans drop, budget compositeContext respecté). ~4 scenarios. Inspire slider Cucumber pipeline | 3 | ✅ TERMINÉ (S-061)
|===

_Total_ : 6 US, ~12 pts. _Méthode_ : baby-step DDD/TDD/BDD, 3 niveaux
tests/US (unit → functional → cucumber), English only code/Gherkin.
_Dépendances_ : MEMPHIS (agent-contracts 0.0.2), codebase (bridge
removal). _Statut_ : 6/6 US TERMINÉ (S-061 + S-062 US-4 stricte). EPIC
PLN-CONTRACTS CLOS.

Planner est désormais aligné sur les contrats N0 : types `Plan`/`Epic`/
`UserStory`/`GradleTask`/`TaskType` consommés depuis `agent-contracts`
(MEMPHIS), `ToolCatalog` object aligné sur les 7 outils de
`vibecoding-contracts.ToolRegistry`, et codebase n'a plus de duplicat
`Plan` local (fusion US-4 stricte S-062 — `codebase.koog.planning.Plan`
supprimé, `contracts.agent.Plan` est la source unique de vérité).