= PROMPT_REPRISE — planner-gradle (Session 012)

*Derniere session* : 011 — Pipeline N0→N3 ABC: GenerateSPGTask LLM Augmenté + Contexte Composite (2026-05-19)

*Mission session suivante* :
- Tester generateSPG avec Ollama réel (deepseek-v4-pro) + composite context engine
- Si LLM OK : pipeline SPG→SPD complet (GenerateSPDTask)
- Si LLM KO : corriger bridge Ollama / timeout
- publishToMavenLocal obligatoire après toute modif

*Etat du build* : compilation OK. 7/7 tests PASS (GenerateSPGTaskTest). 22/26 tests globaux (4 TestKit timeout LLM préexistants).

*Fichiers modifies session 011* :
- GenerateSPGTask.kt — stub → LLM augmenté + composite context + fallback
- PlanningPlugin.kt — workspaceContextFile + ollamaBaseUrl
- GenerateSPGTaskTest.kt — 6 nouveaux tests

*Pipeline N0→N3* :
- Engine (N3) → collectCompositeContext → graphify (N0) + codebase (N1) + codex/training/capsule (N2)
- Planner (N2) → generateSPG consomme composite-context.json → LLM → spg.adoc + metadata.json
