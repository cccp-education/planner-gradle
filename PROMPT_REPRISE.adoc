= PROMPT_REPRISE — planner-gradle (Session 076 terminée — PLN-LIFECYCLE US-2 Wiring)
:date: 2026-08-17

== Session 076 terminée — PLN-LIFECYCLE US-2

*US-2 Wiring surcharge optionnelle* — nouvelle surcharge additive
`IntentionPlanner.plan(state: AgentState.ContextReady, specContents, model, logger): AgentState.Planned`
sur `IntentionPlanner`. Délègue à la surcharge multi-canal existante, wrap via
`LifecycleAdapter.toPlanned`. Fallback défensif `AgentState.Planned(...)` direct si
adapter retourne `null`. `DecomposeIntentionTask` câblé avec flag opt-in
`-Pagent.lifecycle=true` (défaut `false`) qui émet header `[LIFECYCLE]` avant le stdout legacy.
Baby-step TDD strict RED → GREEN → REFACTOR. `:check` VERT 144 tests 0 FAIL
(baseline 139 → +5 unit). 2 functional `@integration` PASSED. `publishToMavenLocal` VERT.
Zéro bump BOM.

## État EPIC PLN-LIFECYCLE — 3/4 US

|===
| US | Statut

| US-0 cadrage | ✅ TERMINÉ (S-074)
| US-1 `LifecycleAdapter` (~8 unit) | ✅ TERMINÉ (S-075)
| US-2 Wiring surcharge optionnelle (~5 unit + 2 functional) | ✅ TERMINÉ (S-076)
| US-3 Cucumber BDD (~4 scénarios) | 🟡 À FAIRE
|===

## État global — EPICs

|===
| EPIC | Statut
| PLN-LIFECYCLE | 🟡 À FAIRE (S-076 — 3/4 US, US-2 Wiring TERMINÉ)
| LLM-ADAPTER-EXTRACT | ✅ TERMINÉ (S-073 — 6/6 US)
| PLN-LLM-HUB | ✅ TERMINÉ (S-072 — 4/4 US)
| PLN-BUDGET | ✅ TERMINÉ (S-071 — 4/4 US)
| PLN-VERIFY | ✅ TERMINÉ (S-067 — 5/5 US)
| PLN-CONTRACTS | ✅ TERMINÉ (S-061 — 6/6 US)
| PLN-VIBE | ✅ TERMINÉ (S-059 — 6/6 US)
|===

## Prochaine session — 077 : PLN-LIFECYCLE US-3 Cucumber BDD

* Mission : créer `planner_lifecycle.feature` avec ~4 scénarios `@lifecycle` :
  (1) intention → ContextReady → Planned via lifecycle adapter,
  (2) fallback path actuel si `AgentState` indisponible (factory throws),
  (3) non-régression 4 String + Logger préservé (flag absent → pas de header),
  (4) flag `-Pagent.lifecycle=true` émet header `[LIFECYCLE]`.
* Pattern shared state : `PlannerScenarioState` + `PlannerScenarioStateReset`
  (pattern S-067 `PlannerVerifySteps`). Steps préfixés "lifecycle" (bug S-088 glue).
* Runner dédié `CucumberTestRunner` existant — feature dans `src/test/features/`.
* Méthode : baby-step BDD strict RED → GREEN → REFACTOR.
* Build : `./gradlew :planner-plugin:check` VERT avant de coder.
* Dépendance : `agent-contracts:0.0.3` déjà dans `build.gradle.kts:29`.
  `LifecycleAdapter` (US-1) + surcharge `plan(ContextReady)` (US-2) disponibles.
* Zéro bump BOM.

Reprendre en lisant `AGENT.adoc`, `.agents/INDEX.adoc`, `BACKLOG.adoc`,
`CADRAGE_EPIC_PLN_LIFECYCLE.adoc`.