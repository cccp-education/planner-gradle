= PROMPT_REPRISE — planner-gradle (Session 060 terminée)
:date: 2026-08-11

== Session 060 terminée — Maintenance @integration (exclusion tests Ollama)

6 tests GradleRunner (`DecomposeIntentionMultiCanalTest` x4 +
`DecomposeIntentionPluginTest` x2) échouaient en CI par `ConnectException`
sur Ollama `localhost:1`. Marqués `@Tag("integration")` (JUnit5) et exclus
du `check` normal via `useJUnitPlatform { excludeTags("integration") }`
sur le `test` task. Tâche `integrationTest` dédiée créée
(`includeTags("integration")`, `excludeEngines("cucumber")`,
`excludeTestsMatching("planning.steps.CucumberTestRunner")`).

Aucun code de production modifié — pure maintenance test infra. Les 2
tests unitaires purs de `DecomposeIntentionMultiCanalTest` et les 2 tests
`tasks`-only de `DecomposeIntentionPluginTest` restent dans `:test`.

`./gradlew :planner-plugin:check` VERT. `publishToMavenLocal` VERT (R2).
EPIC PLN-VIBE reste CLOS (6/6 US).

== Prochaine session — 061 : options

EPIC PLN-VIBE TERMINÉ. Borough planner n'a plus d'EPIC actif.

Options :
. *PUB-3.10* — Publication Maven Central planner-plugin (bloqué : nécessite
  PUB-0/PUB-1 DNS TXT + GPG, dépendances externes).
. *PUB-2.2* — Mise à jour commentaire relocation build.gradle.kts (1 pt,
  P1, pas de dépendance — quick win).
. *Nouvel EPIC* — Cadrage (multi-canal, RAG étendu, contrats N0,
  dogfooding). Besoin d'une direction produit.

Reprendre en lisant `AGENT.adoc`, `.agents/INDEX.adoc`, `BACKLOG.adoc`.
Vérifier `./gradlew :planner-plugin:compileKotlin` VERT avant de coder.