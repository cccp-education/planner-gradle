= PROMPT_REPRISE — planner-gradle (Session 057 terminée)
:date: 2026-08-06

== Session 057 terminée — Cadrage EPIC PLN-VIBE

Cadrage EPIC PLN-VIBE (planner vibecoding-aware) validé par le pilote. Audit
complet planner existant (11 fichiers, ~600 lignes) + code réutilisable 4
boroughs (codebase ToolRegistry/VibecodingPlan/StepVerifier, workspace-bom
AgentState/GradleTask, slider ValidationResult/DeckState invariants).

6 US ~12 pts, baby-step TDD/BDD, English only code/Gherkin, zéro régression.

Gap identifié : `Task` ne modélise que `gradleTask` (pas edit_file/exec_shell).
`ToolRegistry` de codebase sait déjà exécuter 7 outils, mais planner ne les
planifie pas — le prompt produit uniquement des tâches Gradle.

== Prochaine session — 058 : PLN-VIBE-1

- `TaskType` enum (GRADLE, EDIT_FILE, EXEC_SHELL) + étendre `Task` (toolType,
  target optionnel). Invariants `init` (inspiré slider DeckState).
- Baby-step TDD strict : RED (tests échouent — champs absents) -> GREEN (champs
  ajoutés) -> REFACTOR.
- Vérifier `./gradlew :planner-plugin:compileKotlin` VERT.
- Vérifier tests existants PLN-7 (IntentionPlanner/LLMResponse) restent VERT.
- Non-régression : `toolType` défaut `GRADLE` + `target` défaut `""` préserve
  `PlannerIntegration.kt` (codebase) qui appelle `IntentionPlanner.plan()`.