= PROMPT_REPRISE — planner-gradle (Session 079 terminée — SVO-3 adapter PlannerPort cross-borough)
:date: 2026-09-10

== Session 080 — Mission proposée

Borough en position de fournisseur d'adapter pour l'EPIC SVO (pilotage
Queens). Options dans l'ordre :

. **SVO suite côté Queens** : SVO-4 tâche `autonomousSession` se fait côté
   codebase (Queens) — résolution du port par nom canonique
   `plannerPortService` + fallback NoOp. Manhattan en attente de demande.
. **RENAME-7** (2 pts, Vague 3 actée S-215) : prompts LLM `IntentionPlanner`
   ("AFNOR, REAC" → "referential documents", "SPG generated" → "content plan
   generated") + 5 fixtures. 1er baby-step Vague 3 planner.
. **PLN-REVIEW** (7 US ~11 pts, cadré S-077) : P1-1 persistance plan +
   metadata.json, P1-2 ToolCatalog consomme vibecoding-contracts N0,
   P1-3 câbler surcharge lifecycle dans la tâche, P2-4 koog 1.0.0,
   P2-5 flag agent.lifecycle extension, P2-6 purge commentaires français.
. **Cross-borough** : assister un autre borough sur demande pilote.

== Session 079 terminée — SVO-3 (EPIC SVO cross-borough, pilot Queens S-258)

*Côté Manhattan livré* (domaine `planning.adapter` nouveau) :

* `KoogIntentionPlannerAdapter` — implémente le port N1
  `codebase.koog.plannerport.PlannerPort` (créé S-256). Transposition 1:1
  de l'ex-`PlannerIntegration.plan` : `IntentionPlanner.plan` 5-canaux
  (4 sections `CompositeContext` forwardées), classification
  simple/complexe (seuil 80 chars), catch → `PlanState(error=...)`
  (contrat port : graphe owner du fallback).
* `PlannerPortBuildService` + `PlannerPortRegistry` — BuildService Gradle
  exposant le port au build (params sérialisables ne peuvent pas porter un
  objet arbitraire → registry JVM-wide `@Volatile` + service forwarding).
  Nom canonique `plannerPortService` (`SERVICE_NAME`) — la résolution
  codebase SVO-4 cherche ce nom exact.
* `PlanningLlmService.registerPlannerPort()` + wiring `PlanningPlugin`
  (mirror `registerLlmBuildService` PLN-LLM-HUB) — l'adapter est construit
  sur le LLM du hub, inscrit dans le registry.
* Baby-step TDD strict RED → GREEN → REFACTOR. `planning.adapter.*` 9/9.
  `test` global 153 tests 0 FAIL (baseline 144 → +9). `publishToMavenLocal`
  VERT (planner-plugin 0.0.2 republicié). Zéro bump BOM.
* Cycle N1↔N2 reste mort : flèche unique planner → codebase (import
  `codebase.koog.plannerport.*` côté N2, zéro `planning.*` côté N1).

*Reste EPIC SVO* : SVO-4 (Queens) + SVO-5 (Queens). Rapport :
`.agents/sessions/079-svo3-adapter-planner-port.adoc`.

== Historique récent

* S-077 : code review borough (CODE_REVIEW_077.adoc, 9 findings) — EPIC
  PLN-REVIEW créé (7 US ~11 pts).
* S-076 : PLN-LIFECYCLE US-2 wiring lifecycle-aware (surcharge
  `plan(state: AgentState.ContextReady...)`, flag opt-in `-Pagent.lifecycle`).
* S-074 : PLN-LIFECYCLE cadrage (split-brain #3 reporté, doc CADRAGE).
* S-073 : LLM-ADAPTER-EXTRACT TERMINÉ 6/6 (extraction
  `LlmProviderChatModelAdapter` vers codebase N1 — source unique).
* S-072 : PLN-LLM-HUB TERMINÉ 4/4 (planner consomme codebase N1 socle LLM).