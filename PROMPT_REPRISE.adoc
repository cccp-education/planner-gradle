= PROMPT_REPRISE — planner-gradle (Session 069 terminée)
:date: 2026-08-11

== Session 069 terminée — PLN-BUDGET US-1 `BudgetAdapter` (domaine `planning.budget`)

Baby-step TDD strict RED → GREEN → REFACTOR. 9/9 unit PASSED. `:check` VERT
104 tests 0 FAIL (baseline 95 → +9). `publishToMavenLocal` VERT.
`RelevanceContext` (data class 4 String) + `BudgetAdapter` (object pur)
créés dans `planning/budget/BudgetAdapter.kt`. `compute` injectable (défaut
= `RelevanceBudget.compute`) pour testabilité des fallbacks. Fallback :
compute retourne vide ou lance → contenus originaux non tronqués.

## État EPIC PLN-BUDGET — EN COURS (2/4 US)

|===
| US | Statut

| US-0 cadrage | ✅ TERMINÉ (S-068)
| US-1 `BudgetAdapter` (domaine `planning.budget`, object pur) | ✅ TERMINÉ (S-069)
| US-2 Wiring `DecomposeIntentionTask` | ⏳ À FAIRE
| US-3 Cucumber BDD `planner_budget.feature` | ⏳ À FAIRE
|===

## Prochaine session — 070 : PLN-BUDGET US-2 wiring `DecomposeIntentionTask`

Remplacer `ChannelBudget.fromConfig(config)` + `budget.applyBudget(channels)`
(lignes 104-121) par `BudgetAdapter.applyRelevanceBudget(intention,
eagerCtx, ragCtx, graphifyCtx, docsCtx)`. ~5 unit + 2 functional.

* Le bloc actuel (lignes 102-139) est le `try/catch` qui écrit un
  `CompositeContext` typed. US-2 le remplace par le budget dynamique.
* `hasMultiChannel` (ligne 74) reste le déclencheur — le budget dynamique
  ne s'applique que si au moins un canal est fourni.
* Fallback : si `RelevanceBudget` indisponible (exception) → fallback sur
  `ChannelBudget` fixe (path actuel). `BudgetAdapter` gère déjà le fallback
  contenu original si compute vide/exception.
* Backward compat : `CompositeContext`/`CompositeContextConfig` importés
  dans `DecomposeIntentionTask` peuvent être retirés si plus consommés —
  vérifier. Le log `[planner] Multi-canal activé` doit rester.
* Baby-step TDD strict RED → GREEN → REFACTOR requis. English only.
* Après : vérifier `./gradlew :planner-plugin:check` VERT puis
  `./gradlew :planner-plugin:publishToMavenLocal`.

Reprendre en lisant `AGENT.adoc`, `.agents/INDEX.adoc`, `BACKLOG.adoc`.
Vérifier `./gradlew :planner-plugin:compileKotlin` VERT avant de coder.
