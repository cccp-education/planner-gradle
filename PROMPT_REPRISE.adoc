= PROMPT_REPRISE — planner-gradle (Session 075 terminée — PLN-LIFECYCLE US-1 LifecycleAdapter)
:date: 2026-08-17

== Session 075 terminée — PLN-LIFECYCLE US-1

*US-1 `LifecycleAdapter`* (domaine `planning.lifecycle`, 7ème domaine
planner, object pur) — mappe `PlanningContext` + 4 canaux
→ `AgentState.ContextReady` (N0) et `Plan` → `AgentState.Planned` (N0).
Factory injectable pour testabilité des fallbacks (pattern `BudgetAdapter.compute`
S-069). Fallback défensif `null` si factory lance (path actuel préservé).
Baby-step TDD strict RED → GREEN → REFACTOR. `:check` VERT 139 tests 0 FAIL
(baseline 131 → +8). `publishToMavenLocal` VERT. Zéro bump BOM.

## État EPIC PLN-LIFECYCLE — 2/4 US

|===
| US | Statut

| US-0 cadrage | ✅ TERMINÉ (S-074)
| US-1 `LifecycleAdapter` (~8 unit) | ✅ TERMINÉ (S-075)
| US-2 Wiring surcharge optionnelle (~5 unit + 2 functional) | 🟡 À FAIRE
| US-3 Cucumber BDD (~4 scénarios) | 🟡 À FAIRE
|===

## État global — EPICs

|===
| EPIC | Statut
| PLN-LIFECYCLE | 🟡 À FAIRE (S-075 — 2/4 US, US-1 LifecycleAdapter TERMINÉ)
| LLM-ADAPTER-EXTRACT | ✅ TERMINÉ (S-073 — 6/6 US)
| PLN-LLM-HUB | ✅ TERMINÉ (S-072 — 4/4 US)
| PLN-BUDGET | ✅ TERMINÉ (S-071 — 4/4 US)
| PLN-VERIFY | ✅ TERMINÉ (S-067 — 5/5 US)
| PLN-CONTRACTS | ✅ TERMINÉ (S-061 — 6/6 US)
| PLN-VIBE | ✅ TERMINÉ (S-059 — 6/6 US)
|===

## Prochaine session — 076 : PLN-LIFECYCLE US-2 Wiring surcharge optionnelle

* Mission : ajouter une surcharge `plan(state: AgentState.ContextReady, ...): AgentState.Planned`
  à `IntentionPlanner`. Backward compat : la surcharge existante
  `plan(intention, context, specContents, 4 String, model, logger)` est
  préservée. `DecomposeIntentionTask` peut consommer l'API lifecycle-aware si
  `-Pagent.lifecycle=true` (flag optionnel, défaut `false`). ~5 unit + 2 functional.
* Méthode : baby-step TDD strict RED → GREEN → REFACTOR.
* Build : `./gradlew :planner-plugin:compileKotlin` VERT avant de coder
  (wrapper autonome).
* Dépendance : `agent-contracts:0.0.3` déjà dans `build.gradle.kts:29`.
  `LifecycleAdapter` (US-1) déjà disponible dans `planning.lifecycle`.
* Zéro bump BOM.

Reprendre en lisant `AGENT.adoc`, `.agents/INDEX.adoc`, `BACKLOG.adoc`,
`CADRAGE_EPIC_PLN_LIFECYCLE.adoc`.