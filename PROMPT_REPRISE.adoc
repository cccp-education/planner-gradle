= PROMPT_REPRISE — planner-gradle (Session 070 terminée)
:date: 2026-08-12

== Session 070 terminée — PLN-BUDGET US-2 Wiring DecomposeIntentionTask

Baby-step TDD strict RED → GREEN → REFACTOR. 5/5 unit PASSED + 2
functional `@integration`. `:check` VERT 109 tests 0 FAIL (baseline 104
→ +5). `publishToMavenLocal` VERT. `BudgetWiring` (object pur, domaine
`planning.budget`) créé — `resolveBudgetedContexts` délègue à
`BudgetAdapter` (US-1) + log token counts. `DecomposeIntentionTask`
câblé : budget AVANT LLM, log AVANT LLM, contenu budgetisé passé au plan.
Bloc `CompositeContext`/`ChannelBudget` (39 lignes) supprimé. Imports
nettoyés (-3).

## État EPIC PLN-BUDGET — EN COURS (3/4 US)

|===
| US | Statut

| US-0 cadrage | ✅ TERMINÉ (S-068)
| US-1 `BudgetAdapter` (domaine `planning.budget`, object pur) | ✅ TERMINÉ (S-069)
| US-2 Wiring `DecomposeIntentionTask` | ✅ TERMINÉ (S-070)
| US-3 Cucumber BDD `planner_budget.feature` | ⏳ À FAIRE
|===

## Prochaine session — 071 : PLN-BUDGET US-3 Cucumber BDD

`planner_budget.feature` ~4 scénarios `@budget` :
* Budget dynamique alloue plus de tokens au canal pertinent (EAGER >
  RAG/GRAPHIFY/DOCS quand intention match EAGER).
* Fallback contenu original si `compute` retourne vide (non-regression).
* Non-regression 4 canaux préservés (budgeted content passed to LLM).
* Total tokens respecté (somme <= 8000 + tolerance).

Pattern Cucumber shared state `PlannerScenarioState` (S-067) +
`PlannerVerifySteps` (S-067) + `PlannerVibecodingSteps` (S-059).
`CucumberTaskSpec` 4ème cucumber runner.

* Baby-step BDD strict RED → GREEN → REFACTOR requis. English only.
* Après : vérifier `./gradlew :planner-plugin:check` VERT puis
  `./gradlew :planner-plugin:publishToMavenLocal`.

Reprendre en lisant `AGENT.adoc`, `.agents/INDEX.adoc`, `BACKLOG.adoc`.
Vérifier `./gradlew :planner-plugin:compileKotlin` VERT avant de coder.