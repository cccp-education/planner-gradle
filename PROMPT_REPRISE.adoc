= PROMPT_REPRISE — planner-gradle (Session 050 terminée)
:date: 2026-05-22

== Session 050 terminée — Migration SPG vers training (Newark)

Le `GenerateSPGTask` + `FormationModule` + `FormationSession` + `GenerateSPGTaskTest` ont été déplacés
de planner (Manhattan) vers training (Newark). La responsabilité métier formation
ne vit plus dans le plugin d'ordonnancement.

== Ce qui reste dans planner

- `DecomposeIntentionTask` / `generatePlan` — coeur métier planner (ordonnancement)
- `IntentionPlanner` / `SpecReader` / `PlanningContext` / `LLMResponse` / `Plan/Epic/UserStory/Task`
- `OllamaBridge` — conservé pour generatePlan
- `StdoutFormatter`
- `PlannerExtension` — simplifié (retrait de formationsDir)

== Ce qui a quitté planner

- `GenerateSPGTask.kt` → training-plugin (Newark)
- `FormationModule` + `FormationSession` data classes → training-plugin (FormationModels.kt)
- `GenerateSPGTaskTest.kt` → training-plugin
- `formationsDir` dans PlannerExtension → supprimé
- Tâche `generateSPG` dans PlanningPlugin → supprimée

== Appel SPG désormais

```
./gradlew :training-plugin:generateSPG
```
