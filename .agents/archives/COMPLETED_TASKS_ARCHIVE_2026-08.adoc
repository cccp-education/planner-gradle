= COMPLETED_TASKS_ARCHIVE — planner-gradle — Août 2026

|===
| Date | Tâche | Borough | Statut | Score
| 2026-08-06 | Session 057 — Cadrage EPIC PLN-VIBE (planner vibecoding-aware). Audit planner existant + code réutilisable 4 boroughs. 6 US ~12 pts validées. | planner-gradle | ✅ | 9/10
| 2026-08-06 | Session 058 — PLN-VIBE-1/2/3/4 : TaskType enum + Task étendu (toolType, target, 3 invariants init), LLMTask étendu + toTask() internal, prompt enrichi (catalogue 6 outils + cross-borough), StdoutFormatter affiche toolType. 24 nouveaux tests unitaires VERT. | planner-gradle | ✅ | 9/10
| 2026-08-11 | Session 059 — PLN-VIBE-5 Cucumber BDD `planner_vibecoding.feature` 4 scénarios VERT. Infra Cucumber configurée. EPIC PLN-VIBE TERMINÉ 6/6 US. | planner-gradle | ✅ | 9/10
| 2026-08-11 | Session 060 — Maintenance @integration. 6 tests GradleRunner marqués `@Tag("integration")`, exclus du `check`. Tâche `integrationTest` dédiée créée. `:check` VERT. Zéro production modifié. | planner-gradle | ✅ | 9/10
| 2026-08-11 | Session 061 — EPIC PLN-CONTRACTS TERMINÉ 6/6 US. Planner aligné sur contrats N0 : US-0 cadrage (5 split-brain), US-1 BOM agent-contracts 0.0.2 (GradleTask+toolType/target+TaskType enum, 9 unit MEMPHIS), US-2 planner consomme N0 (Plan.kt local supprimé, 24 tests adaptés), US-3 ToolCatalog object (7 outils alignés sur vibecoding-contracts.ToolRegistry, 5 unit), US-4 codebase bridge préserve toolType/target (split-brain #1 résolu), US-5 Cucumber BDD planner_contracts.feature 4 scénarios + non-régression PLN-VIBE-5 4/4. `:check` VERT. Cross-borough MEMPHIS + codebase. | planner-gradle | ✅ | 10/10
| 2026-08-11 | Session 062 — PLN-CONTRACTS US-4 stricte (fusion `codebase.koog.planning.Plan` → `contracts.agent.Plan`). Type `Plan` N1 supprimé (duplicat identique au N0). Bridge `toContractPlan()` supprimé (identité). 10 fichiers codebase imports redirectés. `compileKotlin`/`compileTestKotlin` VERT zéro warning, tests koog + cucumberTestEpicX6/Z7 VERTS. Diff net 2 lignes — le reste était déjà committé dans HEAD (découverte : commits 5af3a56/5f5a1d3/aa7955a avaient fait la fusion sans doc governance). Gouvernance rattrapée. | planner-gradle | ✅ | 9/10
| 2026-08-11 | Session 063 — Cadrage EPIC PLN-VERIFY (VibecodingStep verification metadata). Split-brain #5 reporté S-061. 4 constats audités (VibecodingStep 3 champs / GradleTask 0 / bridge hardcodé / prompt LLM muet). 5 US découpees (US-0 cadrage ✅, US-1 BOM 0.0.3, US-2 planner, US-3 codebase bridge, US-4 Cucumber). Zéro code modifié — session de gouvernance pure (pattern S-057). | planner-gradle | ✅ | 9/10
| 2026-08-11 | Session 064 — PLN-VERIFY US-1 BOM cross-borough. Étendu `contracts.agent.GradleTask` (N0) avec `expectedOutput`/`maxRetries`/`verifyHook` + 2 invariants (non-blank, 1..10). Baby-step TDD RED → GREEN (12 unit `GradleTaskVerifyTest` + 9 non-régression + 31 cucumber VERT). Bump `agent-contracts` 0.0.2 → 0.0.3 (bundle `cd4d2d44`) + BOM 0.0.17 → 0.0.18 (bundle `3d2702bf`). Non-régression planner `:check` VERT contre 0.0.3. Cross-borough MEMPHIS. | planner-gradle | ✅ | 9/10
| 2026-08-11 | Session 065 — PLN-VERIFY US-2 planner consomme N0 étendu. Étendu `LLMTask` (3 champs nullables) + `toTask()` defaults + `IntentionPlanner.buildPromptInternal` (schéma JSON +3 champs + 3 règles prompt) + `StdoutFormatter` (affiche custom expectedOutput, économie d'encre si default). Baby-step TDD strict RED → GREEN → REFACTOR. 3 fichiers test créés (21 unit : 10 `LLMTaskVerifyTest` + 7 `IntentionPlannerVerifyTest` + 4 `StdoutFormatterVerifyTest`). 90 unit + 8 cucumber = 98 PASS, 0 FAIL (baseline 77 → +21). `publishToMavenLocal` VERT. Backward compat préservée : defaults GradleTask identiques si LLM omet. | planner-gradle | ✅ | 9/10
| 2026-08-11 | Session 066 — PLN-VERIFY US-3 codebase cross-borough. Bridge `VibecodingGraph.extractCurrentStep` (ligne 713) rendu `internal` + consomme `task.expectedOutput`/`maxRetries`/`verifyHook` du `GradleTask` N0 (0.0.3) au lieu de hardcoder `"BUILD SUCCESSFUL"`. Site rollback (ligne 741) clarifié (fallback default N0). Bump `agent-contracts` codebase 0.0.2 → 0.0.3. Baby-step TDD strict RED (compile fail `extractCurrentStep` private) → GREEN (9/9 PASSED) → REFACTOR. 1 fichier test créé (9 unit `VibecodingGraphVerifyStepTest`). 881 tests codebase 0 failures. `publishToMavenLocal` VERT (codebase-plugin). Non-régression planner UP-TO-DATE. Cross-borough Queens. | planner-gradle | ✅ | 9/10
| 2026-08-11 | Session 068 — Cadrage EPIC PLN-BUDGET (Relevance Budget Dynamique). Split-brain #4 reporté S-061. Audit : `agent-contracts.RelevanceBudget` (N0, 0.0.3 déjà publié) est un contrat orphan — zéro consommateur. `ChannelBudget` (fixe 40/30/20/10) utilisé par planner (`DecomposeIntentionTask.kt:112`) et codebase pour tronquer post-collecte. 5 constats audités. Décisions : EPIC planner-only, codebase HORS SCOPE (DORMANT), adapter `BudgetAdapter` (domaine `planning.budget`), fallback contenu original si compute vide/exception, zéro bump BOM. 4 US découpees (US-0 cadrage ✅, US-1 `BudgetAdapter` ~8 unit, US-2 wiring ~7 tests, US-3 Cucumber ~4 scénarios). Zéro code modifié — gouvernance pure pattern S-057/S-063. | planner-gradle | ✅ | 9/10
| 2026-08-11 | Session 069 — PLN-BUDGET US-1 `BudgetAdapter` (domaine `planning.budget`). Object pur mappe 4 canaux planner → `Map<String,String>` (EAGER/RAG/GRAPHIFY/DOCS) → `RelevanceBudget.compute(intention, channels, totalTokens=8000)` → `List<ChannelScore>` → `RelevanceContext` (4 String tronquées). `compute` injectable (défaut = `RelevanceBudget.Companion::compute`) pour testabilité des fallbacks. Fallback : compute retourne `channels.isEmpty()` ou lance → contenus originaux (backward compat). Baby-step TDD strict RED (9 `Unresolved reference 'BudgetAdapter'`) → GREEN (9/9 PASSED) → REFACTOR (`:check` VERT 104 tests 0 FAIL, baseline 95 → +9). `publishToMavenLocal` VERT. 2 fichiers créés (1 prod + 1 test). Zéro bump BOM. EPIC PLN-BUDGET 2/4 US. | planner-gradle | ✅ | 10/10
| 2026-08-12 | Session 070 — PLN-BUDGET US-2 Wiring `DecomposeIntentionTask`. `BudgetWiring` (object pur, domaine `planning.budget`) créé — `resolveBudgetedContexts` délègue à `BudgetAdapter` (US-1) + log token counts via `ContextChannel.estimateTokens`. `DecomposeIntentionTask` câblé : budget AVANT LLM, log AVANT LLM, contenu budgetisé passé au plan. Bloc `CompositeContext`/`ChannelBudget` (39 lignes) supprimé. Imports nettoyés (-3). Baby-step TDD strict RED (8 `Unresolved reference 'BudgetWiring'`) → GREEN (5/5 `BudgetWiringTest` PASSED + bug `String.format {}` corrigé interpolation Kotlin) → REFACTOR. `:check` VERT 109 tests 0 FAIL (baseline 104 → +5). `publishToMavenLocal` VERT. 2 fichiers créés (1 prod `BudgetWiring.kt` + 1 test `BudgetWiringTest.kt`) + 1 test functional `DecomposeIntentionBudgetTest.kt` (2 `@integration`). 1 fichier modifié (`DecomposeIntentionTask.kt` -39 lignes). Zéro bump BOM. EPIC PLN-BUDGET 3/4 US. | planner-gradle | ✅ | 9/10
| 2026-08-12 | Session 071 — PLN-BUDGET US-3 Cucumber BDD + EPIC CLOS. `planner_budget.feature` 4 scénarios `@budget` (dynamic allocation EAGER > RAG/GRAPHIFY/DOCS, fallback original content si compute vide, 4 channels preserved + shorter-than-or-equal, total tokens <= 8000 + 1000 tolerance). `PlannerBudgetSteps.kt` créé (13 step defs, pattern `PlannerVerifySteps` S-067). `PlannerScenarioState.kt` étendu (+7 champs budget : intention, 4 contexts, budgetedContext, budgetLogs). Baby-step BDD strict RED → GREEN → REFACTOR. `:check` VERT 113 tests 0 FAIL (baseline 109 → +4 cucumber). `publishToMavenLocal` VERT. Zéro production modifié — pure BDD. EPIC PLN-BUDGET TERMINÉ 4/4 US. | planner-gradle | ✅ | 9/10
| 2026-08-12 | Session 072 — EPIC PLN-LLM-HUB TERMINÉ 4/4 US. Planner consomme codebase (N1) comme socle LLM unifié. US-0 cadrage + US-1 PlanningLlmService + LlmProviderChatModelAdapter (49 lignes copy) + US-2 wiring + suppression OllamaBridge + US-3 Cucumber BDD 6 scénarios. `:check` VERT 132 tests 0 FAIL. `publishToMavenLocal` VERT. Zéro bump BOM. LlmProviderChatModelAdapter dupliqué 3× → EPIC LLM-ADAPTER-EXTRACT découvert. | planner-gradle | ✅ | 9/10
| 2026-08-13 | Session 073 — EPIC LLM-ADAPTER-EXTRACT TERMINÉ 6/6 US + PUB-2.2 CADUQUE + PUB-3.10 publication. Extraction `LlmProviderChatModelAdapter` (49 lignes, 3 copies identiques slider/capsule/planner) vers `codebase.koog.llm.adapter` (N1). US-0 cadrage (6 constats, 10 décisions, doc CADRAGE). US-1 codebase N1 (adapter + 5 unit + `api(libs.langchain4j)` + fix `PlannerIntegration.kt` bug S-072 + bump 0.0.5→0.0.7 + publish Central bundle `e08acb47`). US-2 slider (suppression copie + langchain4j 1.14.1→1.16.3 + codebase 0.0.7). US-3 capsule (suppression copie + codebase 0.0.7). US-4 planner (suppression copie + langchain4j 1.16.3 + codebase 0.0.7 + wrapper autonome). US-5 Cucumber BDD `planner_adapter_extract.feature` 4 scénarios `@adapter-extract` + `PlannerAdapterExtractSteps` (pattern S-088). Baby-step TDD/BDD strict. `:check` VERT 27/27 cucumber PASSED. Cross-borough 4 repos. PUB-2.2 CADUQUE (commentaire RELOCATION supprimé CNV-6). PUB-3.10 planner-plugin 0.0.2 Central bundle `47064425`. | planner-gradle | ✅ | 9/10
| 2026-08-17 | Session 074 — Cadrage EPIC PLN-LIFECYCLE (split-brain #3 AgentState) + maintenance cross-borough. 6 constats (`AgentState`/`ContextAssembler`/`PlanExecutor` N0 orphans, codebase gère lifecycle koog local, planner stateless). 10 décisions (EPIC planner-only, `LifecycleAdapter` domaine `planning.lifecycle`, surcharge optionnelle additive, codebase HORS SCOPE DORMANT, zéro bump BOM agent-contracts 0.0.3 déjà classpath). 4 US ~9 pts. Doc CADRAGE_EPIC_PLN_LIFECYCLE.adoc. Maintenance : publication slider 0.0.18 + BOM 0.0.19. Découverte EPIC L TERMINÉ codebase S-034-049. Zéro code modifié — gouvernance pure. | planner-gradle | ✅ | 9/10
| 2026-08-17 | Session 075 — PLN-LIFECYCLE US-1 `LifecycleAdapter` (domaine `planning.lifecycle`, 7ème domaine planner, object pur). Mappe `PlanningContext` + 4 canaux → `AgentState.ContextReady` (N0) et `Plan` → `AgentState.Planned` (N0). Factory injectable pour testabilité des fallbacks (pattern `BudgetAdapter.compute` S-069). Fallback défensif `null` si factory lance (path actuel préservé). Baby-step TDD strict RED (8 tests → `Unresolved reference 'LifecycleAdapter'`) → GREEN (8/8 PASSED) → REFACTOR (`:check` VERT 139 tests 0 FAIL, baseline 131 → +8). `publishToMavenLocal` VERT. Zéro bump BOM (agent-contracts 0.0.3 déjà classpath). 2 fichiers créés (1 prod `LifecycleAdapter.kt` 67 lignes + 1 test `LifecycleAdapterTest.kt` 132 lignes). Zéro fichier existant modifié. EPIC PLN-LIFECYCLE 2/4 US. | planner-gradle | ✅ | 9/10
| 2026-08-17 | Session 076 — PLN-LIFECYCLE US-2 Wiring surcharge optionnelle. Nouvelle surcharge additive `IntentionPlanner.plan(state: AgentState.ContextReady, specContents, model, logger): AgentState.Planned` délègue à la surcharge multi-canal existante, wrap via `LifecycleAdapter.toPlanned`. Fallback défensif `AgentState.Planned(...)` direct si adapter retourne `null`. `DecomposeIntentionTask` câblé avec flag opt-in `-Pagent.lifecycle=true` (défaut `false`) qui émet header `[LIFECYCLE]` avant le stdout legacy. 2 fichiers prod modifiés (`IntentionPlanner.kt` +44 lignes, `DecomposeIntentionTask.kt` +18 lignes) + 2 fichiers test créés (5 unit `IntentionPlannerLifecycleTest` + 2 functional `DecomposeIntentionLifecycleTest` @integration). Baby-step TDD strict RED → GREEN → REFACTOR. `:check` VERT 144 tests 0 FAIL (baseline 139 → +5 unit). `publishToMavenLocal` VERT. Zéro bump BOM. EPIC PLN-LIFECYCLE 3/4 US. | planner-gradle | ✅ | 9/10
|===

| 2026-08-17 | Session 077 — Code review complète du borough vs vision globale (TAXONOMIE_WORKSPACE, runtime agentique, dashboard, cockpit). 9 findings (3 P1/3 P2/3 P3) documentés `CODE_REVIEW_077.adoc`. EPIC PLN-REVIEW créé dans BACKLOG (7 US ~11 pts : P1-1 persistance plan, P1-2 ToolCatalog vibecoding-contracts N0, P1-3 câblage lifecycle, P2-4 koog 1.0.0, P2-5 flag extension, P2-6 purge FR). Gouvernance pure — zéro code modifié. Rapport créé en rattrapage S-078. | planner-gradle | ✅ | 9/10
| 2026-09-07 | Session 078 — Synchronisation gouvernance post-S-077. Rapports S-074/S-077 créés en rattrapage, INDEX aligné, PROMPT_REPRISE mission 079. BACKLOG : diff S-077 inclus au commit sync. Gouvernance pure — zéro code. Baseline S-076 préservée (144 tests 0 FAIL). | planner-gradle | ✅ | 9/10
|===
